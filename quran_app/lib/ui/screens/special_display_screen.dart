import 'package:flutter/material.dart';

/// A placeholder screen for the upcoming "Special Display" feature.
///
/// Currently shows an empty state message. Replace this with real
/// content when the feature requirements are finalized.
class SpecialDisplayScreen extends StatelessWidget {
  const SpecialDisplayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // Using Center ensures the message is nicely positioned regardless of screen size.
    return const Center(child: Text('Special Display coming soon!'));
  }
}
